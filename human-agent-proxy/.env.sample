# Email Connector Configuration
# Copy this file to .env and fill in your actual values

# ============================================
# SMTP Configuration (for sending emails)
# ============================================

# SMTP server hostname
# Examples: smtp.gmail.com, smtp.outlook.com, smtp.mailgun.org
SMTP_HOST=smtp.gmail.com

# SMTP server port
# Common ports: 587 (TLS), 465 (SSL), 25 (unencrypted, not recommended)
# For Gmail: use 587 with secure=false, or 465 with secure=true
SMTP_PORT=587

# SMTP secure flag
# true = use SSL/TLS (typically for port 465)
# false = use STARTTLS (typically for port 587)
SMTP_SECURE=false

# SMTP authentication username (usually your email address)
SMTP_USER=your-email@gmail.com

# SMTP authentication password
# For Gmail: use an App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_PASS=your-app-password

# ============================================
# IMAP Configuration (for receiving emails)
# ============================================

# IMAP server hostname
# Examples: imap.gmail.com, imap.outlook.com, imap.mailgun.org
IMAP_HOST=imap.gmail.com

# IMAP server port
# Common ports: 993 (SSL/TLS), 143 (STARTTLS)
# For Gmail: use 993 with tls=true
IMAP_PORT=993

# IMAP authentication username (usually your email address)
# Can be the same as SMTP_USER if using the same email account
IMAP_USER=your-email@gmail.com

# IMAP authentication password
# For Gmail: use an App Password (not your regular password)
# Can be the same as SMTP_PASS if using the same email account
IMAP_PASS=your-app-password

# IMAP TLS flag
# true = use SSL/TLS encryption (recommended, typically for port 993)
# false = use unencrypted connection (not recommended)
IMAP_TLS=true

# ============================================
# Additional Notes
# ============================================
# 
# For Gmail:
# 1. Enable 2-factor authentication
# 2. Generate an App Password at: https://myaccount.google.com/apppasswords
# 3. Use the App Password (16 characters) as SMTP_PASS and IMAP_PASS
# 
# For other providers:
# - Check your email provider's documentation for SMTP/IMAP settings
# - Some providers may require specific ports or security settings
# - Corporate email may require additional configuration
